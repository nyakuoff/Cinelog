# Default DATABASE_URL so `prisma generate` / `migrate` resolve the datasource
# when no env is otherwise set. Real environment variables (dev root .env,
# CI, Docker container env) take precedence — dotenv never overrides them.
DATABASE_URL="file:../data/cinelog.db"
